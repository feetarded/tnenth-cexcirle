#dont ask why this exists it had reasoning before
#probably to consolidate all the constants that needed globalling or sum
const stat_registry = preload('res://scripts/Classes/Entity/stat_registry.gd').ReferenceIDs
